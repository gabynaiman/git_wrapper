language: ruby
rvm:
  - 1.9.3
  - 2.0
  - 2.1
  - 2.2
  - 2.3.0
  - 2.4.0
  - jruby
before_install:
  - gem install bundler
before_script:
  - git config --global user.name "User Name"
  - git config --global user.email "user@mail.com"
